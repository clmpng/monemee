# =============================================================================
# MONEMEE - Production Environment Variables
# =============================================================================
# Kopiere diese Datei nach .env und fülle alle Werte aus
# cp .env.production.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=5000
CLIENT_URL=https://monemee.app

# Optional: Mission Control URL (Admin Dashboard)
# MISSION_CONTROL_URL=https://admin.monemee.app

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
DB_USER=monemee
DB_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD
DB_NAME=monemee
# Hinweis: DB_HOST und DB_PORT werden automatisch von Docker gesetzt

# -----------------------------------------------------------------------------
# Firebase Admin SDK (Server)
# -----------------------------------------------------------------------------
# Diese Werte findest du in der Firebase Console:
# Project Settings → Service Accounts → Generate New Private Key
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# WICHTIG: Der Private Key muss als ein String mit \n für Zeilenumbrüche sein
# Beispiel: "-----BEGIN PRIVATE KEY-----\nMIIEvgIBADANBg...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Storage Bucket (optional, nur wenn Cloud Storage genutzt wird)
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# -----------------------------------------------------------------------------
# Firebase Client (für React Build)
# -----------------------------------------------------------------------------
# Diese Werte findest du in der Firebase Console:
# Project Settings → General → Your apps → Web app
REACT_APP_FIREBASE_API_KEY=AIzaSy...
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789
REACT_APP_FIREBASE_APP_ID=1:123456789:web:abcdef

# -----------------------------------------------------------------------------
# Stripe (Payment Processing)
# -----------------------------------------------------------------------------
# STRIPE_MODE: "test" für Testmodus, "live" für Produktion
STRIPE_MODE=live

# Test Keys (für Entwicklung)
STRIPE_SECRET_KEY_TEST=sk_test_...
STRIPE_PUBLISHABLE_KEY_TEST=pk_test_...

# Live Keys (für Produktion)
STRIPE_SECRET_KEY_LIVE=sk_live_...
STRIPE_PUBLISHABLE_KEY_LIVE=pk_live_...

# Webhook Secrets (aus Stripe Dashboard → Webhooks)
# Payment Webhook: checkout.session.completed, payment_intent.*
STRIPE_PAYMENT_WEBHOOK_SECRET=whsec_...

# Connect Webhook: account.updated, account.application.deauthorized
STRIPE_CONNECT_WEBHOOK_SECRET=whsec_...

# -----------------------------------------------------------------------------
# Email Service (wähle EINE Option)
# -----------------------------------------------------------------------------

# Option 1: Resend (empfohlen - einfach zu konfigurieren)
RESEND_API_KEY=re_...

# Option 2: SMTP (z.B. Mailgun, SendGrid, oder eigener Server)
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@mg.yourdomain.com
# SMTP_PASS=your-smtp-password

# Absender-Email
EMAIL_FROM=noreply@monemee.app

# -----------------------------------------------------------------------------
# Mission Control (Admin Dashboard)
# -----------------------------------------------------------------------------

# URL fuer Mission Control (Subdomain)
MISSION_CONTROL_URL=https://admin.monemee.app

# Separates JWT Secret fuer Admin-Authentifizierung
# Generiere mit: openssl rand -base64 32
MC_JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# -----------------------------------------------------------------------------
# Staging-Umgebung (nur auf Server benoetigt)
# -----------------------------------------------------------------------------

# Staging URLs
STAGING_URL=https://staging.monemee.app
STAGING_MC_URL=https://admin-staging.monemee.app

# Staging Datenbank (separates Passwort!)
DB_PASSWORD_STAGING=STAGING_DB_PASSWORD_HERE

# Staging Mission Control JWT
MC_JWT_SECRET_STAGING=STAGING_JWT_SECRET_HERE

# Staging Stripe Webhooks (separate Endpoints in Stripe Dashboard!)
STRIPE_PAYMENT_WEBHOOK_SECRET_STAGING=whsec_staging_...
STRIPE_CONNECT_WEBHOOK_SECRET_STAGING=whsec_staging_...

# -----------------------------------------------------------------------------
# Optional: Additional Configuration
# -----------------------------------------------------------------------------

# JWT Secret (falls verwendet)
# JWT_SECRET=your-jwt-secret-here

# Logging Level (debug, info, warn, error)
# LOG_LEVEL=info
